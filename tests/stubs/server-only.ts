// Stub pro import 'server-only' durante os testes (Vitest roda em Node puro,
// fora do bundler do Next.js, que normalmente resolve esse pacote internamente
// — ver node_modules/next/dist/compiled/server-only). Não precisa fazer nada.
export {}
